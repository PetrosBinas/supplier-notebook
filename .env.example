# === Django ===
DJANGO_SECRET_KEY=replace-me
DJANGO_DEBUG=True

# === Gmail OAuth ===
# these are the paths INSIDE the container 
GMAIL_CREDENTIALS_PATH=/run/secrets/gmail_credentials.json
GMAIL_TOKEN_PATH=/run/secrets/gmail_token.json
GMAIL_FROM_ADDRESS=me@mymail.com

# === OAuth helper service ===
OAUTH_PORT=8765
